#include <bits/stdc++.h>

int main() {
    vector<int> arr;
    for (int idx = 0; idx < 10; idx++) {
        arr.push_back(idx)
    }
    for (int i = 0; i < arr.size(); i++) {
        std::cout << arr[i] << " ";
    }
    std::cout << std::endl;
}
